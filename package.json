{
  "devDependencies": {
    "@octokit/core": "^5.1.0",
    "pretty": "^2.0.0",
    "rimraf": "^5.0.5"
  },
  "scripts": {
    "start": "python bmsnavserver.py",
    "start:mac": "python3 bmsnavserver.py",
    "package": "pyinstaller --onefile bmsnavserver.py --windowed",
    "clean": "rimraf build && rimraf dist && rimraf serve && rimraf bmsnavserver.spec",
    "html": "python tools\\readme-to-html.py",
    "html:mac": "python3 tools/readme-to-html.py"
  }
}
